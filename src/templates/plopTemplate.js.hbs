import React from 'react';
import { createIcon, Icon } from "@chakra-ui/icons";

{{!-- const {{name}} = props => {
  return (
    <Icon {...props}>
      {{path}}
    </Icon>
  );
};

export default {{name}}; --}}

export const {{name}} = createIcon({
  displayName: "{{name}}",
  path: ({{path}})
})